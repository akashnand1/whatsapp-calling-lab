{
  "name": "WhatsApp Calling Lab",

  // Debian bookworm + Python 3.11. aiortc and av ship manylinux wheels, so
  // nothing here needs compiling -- the container comes up in about a minute.
  "image": "mcr.microsoft.com/devcontainers/python:3.11-bookworm",

  // 16GB is a MEASURED requirement, not a preference. On the 8GB machine the
  // process was OOM-killed with a bare "Terminated" while loading the streaming
  // recogniser: NeMo + torch is ~2GB resident, the 0.6B model in float32 is
  // ~2.4GB (cache-aware models cannot use fp16), the .nemo archive extracts to
  // 2.37GB on disk first, and Piper's onnxruntime plus the VS Code server were
  // already holding over a gigabyte.
  //
  // Cost: free Codespaces gives 120 core-hours/month, so 4 cores means ~30
  // hours instead of ~60. To go back to the cheaper machine, set this to
  // 2 / "8gb" AND set STT_ENGINE=whisper in .env — Nemotron will refuse to load
  // below ~5GB free rather than risk being killed mid-call.
  "hostRequirements": {
    "cpus": 4,
    "memory": "16gb"
  },

  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {}
  },

  // espeak-ng: Piper phonemises through it.
  // ffmpeg:    PyAV links its own, but the CLI is useful for inspecting audio.
  // libsrtp2:  SRTP for the media leg.
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y --no-install-recommends espeak-ng ffmpeg libsrtp2-1 && bash .devcontainer/setup.sh",

  // 8000 must be PUBLIC, not private. Meta's webhook is an unauthenticated
  // POST from Facebook's servers; a private port would 302 them to a GitHub
  // login page and the SDP answer would never arrive. If your organisation
  // forbids public ports this line is silently overridden -- check the PORTS
  // tab shows "Public" before registering the URL with Meta.
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "whatsapp-lab",
      "visibility": "public",
      "onAutoForward": "notify"
    }
  },

  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python"
      }
    }
  },

  "remoteUser": "vscode"
}
